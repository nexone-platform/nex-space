# Dependencies
node_modules/
.pnpm-store/

# Production build outputs
dist/
build/
.output/

# Environment & Secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Operating System & IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# raw PixelLab asset library (curation source, not deployed/committed)
apps/web/asset-library/

# uploaded files and the key that signs their links — user data and a secret,
# created automatically by apps/api and mounted from a volume in production
apps/api/data/

# local dev sqlite db (created automatically by apps/api)
apps/api/prisma/dev.db
apps/api/prisma/*.db-journal
backups/
